export class ErrorModel {
    code;
    message;

    constructor(code, message) {
        this.code = code;
        this.message = message;
    }
}

export class SuccessModel {
    code;
    message;

    constructor(code, message) {
        this.code = code;
        this.message = message;
    }
}
